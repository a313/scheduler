class Reminder {
  final int id;
  final String name;
  final DateTime createDate;
  final int index;
  final String? image;

  Reminder(this.id, this.name, this.createDate, this.index, this.image);
}
