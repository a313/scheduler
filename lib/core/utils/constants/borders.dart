import 'package:flutter/material.dart';

const borRad02 = BorderRadius.all(Radius.circular(2.0));
const borRad04 = BorderRadius.all(Radius.circular(4.0));
const borRad08 = BorderRadius.all(Radius.circular(8.0));
const borRad12 = BorderRadius.all(Radius.circular(12.0));
const borRad16 = BorderRadius.all(Radius.circular(16.0));
const borRad18 = BorderRadius.all(Radius.circular(18.0));
const borRad24 = BorderRadius.all(Radius.circular(24.0));
const borRad100 = BorderRadius.all(Radius.circular(100.0));
const C1W = 36.0;
const R1H = 50.0;
